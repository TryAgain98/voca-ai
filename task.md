- Công việc tiếp theo:
  +, đối với mỗi lib hãy cấu hình nó
  +, trong app t muốn m chia các folder: stucture cho repo
  +, ví dụ như components, pages, hooks ...
  +, tạo 1 màn home: màn này sẽ sử dụng hết all thư viện để demo các cấu hình vừa setup và xem các lib có hoạt dộng ko
